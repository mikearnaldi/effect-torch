{
  "name": "effect-torch",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "bench": "pnpm -r build && pnpm --filter @effect-torch/bench bench",
    "bench:mlx": "pnpm -r build && pnpm --filter @effect-torch/bench bench:mlx"
  }
}
