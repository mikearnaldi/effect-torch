{
  "name": "@effect-torch/native",
  "version": "0.1.0",
  "type": "commonjs",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "binaryName": "effect-torch-native"
  },
  "scripts": {
    "build": "napi build --release",
    "build:debug": "napi build",
    "build:cuda": "napi build --release --features cuda",
    "build:nccl": "napi build --release --features nccl"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.0.0"
  }
}
